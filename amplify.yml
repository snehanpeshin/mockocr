version: 1
applications:
  - appRoot: frontend
    env:
      variables:
        NEXT_PUBLIC_API_BASE_URL: https://mo-9f59128d1e0048feab5efaaaa71df90c.ecs.us-east-1.on.aws
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: out
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
